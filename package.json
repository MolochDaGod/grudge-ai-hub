{
  "name": "grudge-legion-ai",
  "version": "1.6.0",
  "private": true,
  "description": "GRUDA Legion AI Hub — Cloudflare Worker gateway for Grudge Studio (Workers AI + Gemini BYOK + Groq + agent skills + fleet context)",
  "scripts": {
    "deploy": "wrangler deploy && wrangler deploy --config wrangler.domain.toml",
    "deploy:api": "wrangler deploy",
    "deploy:domain": "wrangler deploy --config wrangler.domain.toml",
    "deploy:hard": "npm run deploy && npm run db:skills:all && npm run smoke",
    "db:skills": "wrangler d1 execute grudge-ai-hub --remote --file=migrations/004_puter_fleet_skills.sql",
    "db:skills:context": "wrangler d1 execute grudge-ai-hub --remote --file=migrations/005_context_agentic_skills.sql",
    "db:skills:all": "npm run db:skills && npm run db:skills:context",
    "smoke": "node scripts/smoke-deploy.mjs",
    "tail": "wrangler tail grudge-legion-ai",
    "tail:domain": "wrangler tail grudge-ai-hub"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}